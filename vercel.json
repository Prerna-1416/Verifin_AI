{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm turbo build --filter=@verifin-ai/web",
  "outputDirectory": "apps/web/.next",
  "regions": ["iad1"]
}
